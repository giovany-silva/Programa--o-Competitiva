#include <malloc.h>
#include <stdlib.h>
#include <string.h>
char *string(char *s,char *a);
char *bolha(char *s, int n);
int main()
{
int n,t;
char* a=malloc(sizeof(char)*1000);
scanf("%d",&n);


while(n--)
    {
     scanf("%s",a);
printf("I am Toorg!\n");        
    }
  return 0;
}
