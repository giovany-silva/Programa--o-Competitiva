#include  <stdio.h>  /**< inclui as funções padrões de entrada e saída. */
#include <stdlib.h>  /**< inlcui as funções manipuladoras de memória. */
#include <string.h>  /**< inclui as funções manipuladoras de strings. */


int main(int argc, char **argv)
{
int N,i,S,T,Q;
do{scanf("%d"N);}while(N<2||N>100000);
int A[N-1],L[N-1];
for(i=1;i<=N-1;i++)
{do{scanf("%d",&A[i]);}while(Ai<0||Ai>i-1);
do{
    scanf("%d",&L[i]);
}while(Li<1||Ai>1000000000);

   do {scanf("%d",&Q);}while(Q<1||Q>100000)
    for(i=0;i<Q;i++)
    {

        do
    }
}

}

